<UserControl x:Class="VMS.TPS.ReporteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="500" Background="White">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Reporte de Índices QA - Radiocirugía" 
                   FontSize="20" FontWeight="Bold" Foreground="#005A9C" Margin="0,0,0,15"/>

        <Border Grid.Row="1" BorderBrush="#DDDDDD" BorderThickness="1" Padding="10" Margin="0,0,0,15" Background="#F9F9F9">
            <StackPanel>
                <TextBlock x:Name="txtPatientName" Text="Paciente: --" FontWeight="SemiBold"/>
                <TextBlock x:Name="txtPlanInfo" Text="Plan: --" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="2" x:Name="gridResultados" AutoGenerateColumns="False" 
                  CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal" Background="White" BorderBrush="#EEEEEE">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Parámetro / Índice" Binding="{Binding Nombre}" Width="2*" FontWeight="SemiBold"/>
                <DataGridTextColumn Header="Valor" Binding="{Binding Valor}" Width="*" />
                <DataGridTextColumn Header="Referencia" Binding="{Binding Referencia}" Width="*" Foreground="Gray" FontStyle="Italic"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Copiar al Portapapeles" Height="30" Width="150" Click="BtnCopiar_Click" 
                    Background="#007ACC" Foreground="White" FontWeight="Bold" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</UserControl>