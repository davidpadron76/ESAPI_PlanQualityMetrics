<UserControl x:Class="VMS.TPS.ReporteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="560" Background="White">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Reporte de Índices QA - Radiocirugía"
                   FontSize="20" FontWeight="Bold" Foreground="#005A9C" Margin="0,0,0,15"/>

        <Border Grid.Row="1" BorderBrush="#DDDDDD" BorderThickness="1" Padding="10" Margin="0,0,0,15" Background="#F9F9F9">
            <StackPanel>
                <TextBlock x:Name="txtPatientName" Text="Paciente: --" FontWeight="SemiBold"/>
                <TextBlock x:Name="txtPlanInfo" Text="Plan: --" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="2" x:Name="gridResultados" AutoGenerateColumns="False"
                  CanUserAddRows="False" CanUserSortColumns="False" IsReadOnly="True" HeadersVisibility="Column"
                  GridLinesVisibility="All" HorizontalGridLinesBrush="#EEEEEE" VerticalGridLinesBrush="#EEEEEE"
                  Background="White" BorderBrush="#DDDDDD" BorderThickness="1"
                  RowBackground="White" AlternatingRowBackground="#F4F8FB"
                  RowHeaderWidth="0" ColumnWidth="*">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Parámetro / Índice" Binding="{Binding Nombre}" Width="2.2*" FontWeight="SemiBold">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Padding" Value="6,4"/>
                            <Setter Property="TextWrapping" Value="Wrap"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Valor" Binding="{Binding Valor}" Width="1.1*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Padding" Value="6,4"/>
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Referencia" Binding="{Binding Referencia}" Width="1.7*" Foreground="Gray" FontStyle="Italic">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Padding" Value="6,4"/>
                            <Setter Property="TextWrapping" Value="Wrap"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>

            <DataGrid.GroupStyle>
                <GroupStyle>
                    <GroupStyle.ContainerStyle>
                        <Style TargetType="{x:Type GroupItem}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type GroupItem}">
                                        <Expander IsExpanded="True" Background="Transparent" BorderThickness="0" Focusable="False">
                                            <Expander.Header>
                                                <Border Background="#005A9C" Padding="8,4" Margin="0,10,0,2" CornerRadius="3">
                                                    <TextBlock Text="{Binding Name}" Foreground="White" FontWeight="Bold" FontSize="12.5"/>
                                                </Border>
                                            </Expander.Header>
                                            <ItemsPresenter/>
                                        </Expander>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </GroupStyle.ContainerStyle>
                </GroupStyle>
            </DataGrid.GroupStyle>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Copiar al Portapapeles" Height="30" Width="150" Click="BtnCopiar_Click"
                    Background="#007ACC" Foreground="White" FontWeight="Bold" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</UserControl>